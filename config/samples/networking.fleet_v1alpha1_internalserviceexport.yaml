apiVersion: networking.fleet.azure.com/v1alpha1
kind: InternalServiceExport
metadata:
  name: example-svc
spec:
  originClusterId: example-cluster
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080

